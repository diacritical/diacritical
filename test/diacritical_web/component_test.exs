defmodule DiacriticalWeb.ComponentTest do
  @moduledoc "Defines an `ExUnit.Case` case."
  @moduledoc since: "0.21.0"

  use DiacriticalCase.View, async: true

  alias DiacriticalWeb

  alias DiacriticalWeb.Component

  doctest Component
end
