defmodule DiacriticalSchemaTest do
  @moduledoc "Defines an `ExUnit.Case` case."
  @moduledoc since: "0.9.0"

  use ExUnit.Case, async: true

  alias DiacriticalSchema

  doctest DiacriticalSchema
end
