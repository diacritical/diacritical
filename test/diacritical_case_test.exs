defmodule DiacriticalCaseTest do
  @moduledoc "Defines an `ExUnit.Case` case."
  @moduledoc since: "0.3.0"

  use ExUnit.Case, async: true

  alias DiacriticalCase

  doctest DiacriticalCase, import: true
end
